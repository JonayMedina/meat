twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes: ['bootstrap_4_layout.html.twig']
    globals:
        app_name: '%app_name%'
        dev_name: '%dev_name%'
        epay_merchant: '%env(EPAY_MERCHANT)%'
